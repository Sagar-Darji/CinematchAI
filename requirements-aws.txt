# CinematchAI — AWS Lambda Requirements
# No torch / sentence-transformers / CLIP — embeddings via Voyage AI API.
# Slimmed for fast Lambda container builds.

# Core LangChain & Agents
langchain==0.3.13
langchain-groq==0.2.1
langgraph==0.2.60

# Embeddings — Voyage AI API (replaces sentence-transformers)
voyageai>=0.2.3

# Vector Database (Qdrant primary — ChromaDB not used on Lambda, no persistent disk)
qdrant-client>=1.9.0
huggingface_hub<0.28.0  # pin to avoid hf-xet Rust build dep on Lambda

# API & Backend
fastapi==0.115.6
uvicorn[standard]==0.34.0
mangum>=0.17.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
google-auth==2.28.0
psycopg2-binary==2.9.9

# Data Processing
pandas==2.2.3
numpy<2.0
pyarrow==18.1.0
Pillow==11.0.0

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.2
requests==2.32.3
aiohttp==3.11.11
python-multipart==0.0.20

# Caching
diskcache==5.6.3

# Logging
loguru==0.7.3

# Observability
prometheus-fastapi-instrumentator==7.1.0
python-json-logger==3.3.0

# Rate Limiting
slowapi==0.1.9

# TMDB API
tmdbv3api==1.9.0
